header 
  div(class="container")
    div(class="header-box")
      a(class="btn btn-lg ion-navicon-round burger-menu")
      nav 
        a(href="/") Рассылки
        a(href="/groups") Группы
        a(href="/addresses") Адреса
        a(href="/templates") Шаблоны
        if user.role < 3
          a(href="/transports") Ящики
        if user.role < 2
          a(href="/users") Пользователи

      div 
        a(href="/auth/logout" class='btn btn-dark btn' title="Выход")= user.name
          span(class="ion-log-out ms-2")
        